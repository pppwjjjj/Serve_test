# ============================================================
# GitHub API 自动化测试框架 —— 依赖清单
# ============================================================
# 安装方式：pip install -r requirements.txt
# ============================================================

# --- HTTP 请求底层库 ---
# 发 HTTP 请求全靠它，core 的底层依赖
requests>=2.31.0

# --- 测试框架 ---
# 编写和运行测试用例
pytest>=8.0.0

# --- 环境变量管理（可选）---
# 让 .env 文件里的配置自动注入到环境变量中
# 不安装也能用，但就得手动 export 环境变量了
python-dotenv>=1.0.0

# --- 测试报告 ---
# HTML 报告（看着比命令行舒服）
pytest-html>=4.1.0

# Allure 报告（生成原始数据，由 Jenkins Allure 插件或 allure CLI 转成 HTML）
allure-pytest>=2.13.0
